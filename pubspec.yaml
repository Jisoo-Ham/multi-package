name: multi_package_workspace
description: Root workspace for clean architecture Flutter project.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  melos: ^7.3.0
